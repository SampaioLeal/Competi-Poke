import { createMuiTheme } from "@material-ui/core";

const pokeTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#7E7394",
    },
    secondary: {
      main: "#524153",
    },
  },
});

export default pokeTheme;
